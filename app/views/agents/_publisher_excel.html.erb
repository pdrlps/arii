<div class="row">
  <div class="small-3 columns">
    <label class="right inline">URI</label>
  </div>
  <div class="small-9 columns">
    <i class="label info lifebuoy">Provide the publisher URI.</i>
    <input id="agent_payload_uri" tabindex="<%= autotab %>" value="<%= @agent.uri.nil? ? '' : @agent.uri %>"  name="agent[uri]" type="text" placeholder="E.g.: https://ariip.com/demo.xls" />
  </div>
</div>
<div class="row">
  <div class="small-3 columns">
    <label class="right inline">Headers</label>
  </div>
  <div class="small-9 columns">
    <i class="label info lifebuoy">Provide the number of header rows in your Excel file.</i>
    <input id="agent_payload_headers" tabindex="<%= autotab %>" <%= @agent.headers ? '' : @agent.headers %>  name="agent[headers]" type="text" placeholder="E.g.: 2"  />
  </div>
</div>
<div class="row">
  <div class="small-3 columns">
    <label class="right inline">Sheet</label>
  </div>
  <div class="small-9 columns">
    <i class="label info lifebuoy">Provide the Excel sheet where the data are.</i>
    <input id="agent_payload_sheet" value="<%= @agent.sheet.nil? ? '' : @agent.sheet %>"  name="agent[sheet]" type="text" placeholder="E.g.: Data"  />
  </div>
</div>
<div class="row">
  <div class="small-3 columns">
    <label class="right inline">Cache</label>
  </div>
  <div class="small-9 columns">
    <i class="label info lifebuoy">Specify the caching element of your dataset.<br />This the column number from your Excel sheet that will be used to verify if each row is unique.</i>
    <input id="agent_payload_cache" tabindex="<%= autotab %>" value="<%= @agent.cache.nil? ? '' : @agent.cache %>"  name="agent[cache]" type="number" placeholder="E.g.: 0"  />
  </div>
</div>
<div class="row">
  <div class="small-3 columns">
    &nbsp;
  </div>
  <div class="small-9 columns">
    <i class="label info lifebuoy text-left">Configure your selectors.<br />Selectors are single data extraction elements that will be passed to the outputs configuration.<br />Keys are the variable names and Values are Excel sheet column numbers.</i>
  </div>
</div>
<%= render :partial => "agents/edit_selectors", locals: {:agent => @agent} %>